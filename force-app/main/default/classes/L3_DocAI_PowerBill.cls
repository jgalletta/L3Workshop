/**
 * Typed schema for the 'powerBill' IDP config.
 * Property name MUST exactly match the config schemaConfig key (address).
 */
public class L3_DocAI_PowerBill {
    @AuraEnabled public L3_DocAI_Field address;
}