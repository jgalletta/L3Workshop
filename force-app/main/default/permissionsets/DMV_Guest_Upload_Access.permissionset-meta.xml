<?xml version="1.0" encoding="UTF-8"?>
<PermissionSet xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>Lets the unauthenticated DMV Portal guest user invoke the DLApplicationUploadResource Apex REST endpoint. That class runs without sharing and creates the Application, ContentVersions and links itself, so the guest needs no direct object/FLS access.</description>
    <hasActivationRequired>false</hasActivationRequired>
    <label>DMV Guest Upload Access</label>
    <classAccesses>
        <apexClass>DLApplicationUploadResource</apexClass>
        <enabled>true</enabled>
    </classAccesses>
</PermissionSet>
